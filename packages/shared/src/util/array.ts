/**
 * Join array using separator
 *
 * @export
 * @param {string[]} s
 * @param {string} [sep='']
 * @returns {string}
 */
export function join(s: string[], sep = ''): string {
  return s.join(sep);
}
