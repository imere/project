autoOpen: false # 打开工作空间时是否自动开启所有应用的预览
apps:
  - port: 8000 # 应用的端口
    run: npm start # 应用的启动命令
    root: ./packages/client # 应用的启动目录
    name: client # 应用名称
    description: client # 应用描述
    autoOpen: true # 打开工作空间时是否自动开启预览（优先级高于根级 autoOpen）
